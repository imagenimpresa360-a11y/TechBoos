{
  "name": "boos-erp-final",
  "version": "1.0.0",
  "description": "ERP The Boos Box - Cloud Edition",
  "main": "gym-platform/backend/server.js",
  "scripts": {
    "install-all": "npm install && cd gym-platform && npm install",
    "build": "cd gym-platform && npm install && npm run build",
    "start": "node gym-platform/backend/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "pg": "^8.11.3",
    "dotenv": "^16.3.1",
    "multer": "^1.4.5-lts.1",
    "xlsx": "^0.18.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
